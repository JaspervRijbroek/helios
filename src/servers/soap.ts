/**
 * Soap is what it is, a soap server with nothing added to it,
 * I want to keep this one as small as possible while also having the option for authentication.
 *
 * As this will also be a part of the whole.
 */
import AbstractServer from "./abstract";

export default class SoapServer extends AbstractServer
{

}