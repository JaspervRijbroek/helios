/**
 * This is a complete different server as it will include XMPP, which is a communication protocol
 */
import AbstractServer from "./abstract";

export default class ChatServer extends AbstractServer
{

}